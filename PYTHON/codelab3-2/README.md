# [Codelab 3-2] 지수 백오프 기반 무차별 대입 공격 차단

## 요구사항

- 가이드
  - bcrypt를 사용하되, 메모리(Dictionary)에 IP별 실패 횟수를 기록
  - 연속 실패 시 지수 백오프(Exponential Backoff)를 적용하여 무차별 대입 공격을 물리적으로 차단하는 알고리즘을 설계
- 라이브러리
  - `import bcrypt`
  - `import time`

## 핵심 개념

### bcrypt

비밀번호를 단방향 해시로 저장하는 라이브러리다. 같은 비밀번호라도 매번 다른 해시값이 나오기 때문에 레인보우 테이블 공격에 강하다.

```python
# 해싱 (저장 시)
hashed = bcrypt.hashpw(password.encode(), bcrypt.gensalt())

# 검증 (로그인 시)
bcrypt.checkpw(input_password.encode(), hashed)
```

### IP별 실패 기록 (메모리)

로그인 실패 정보를 딕셔너리에 IP 단위로 관리한다.

```python
ip_tracker = {
    "192.168.0.99": {
        "attempts": 7,
        "blocked_until": 1743251234.5  # time.time() 기준 차단 해제 시각
    }
}
```

### 지수 백오프 (Exponential Backoff)

연속 실패 횟수가 늘어날수록 대기 시간이 2배씩 증가한다. 공격자 입장에서 시도 횟수가 많아질수록 기다리는 시간이 기하급수적으로 늘어난다.

| 실패 횟수 | 대기 시간 |
|-----------|-----------|
| 5회       | 1초       |
| 6회       | 2초       |
| 7회       | 4초       |
| 8회       | 8초       |
| 9회       | 16초      |
| 10회      | 32초      |

## 실행 결과

```
=======================================================
시나리오 1. 정상 로그인
=======================================================
  [실패] 192.168.0.1 - 1회 실패 (5회 초과 시 차단 시작)
  [성공] 192.168.0.1 - 로그인 성공 ✓

=======================================================
시나리오 2. 무차별 대입 공격 (192.168.0.99)
=======================================================

  [시도 1회]
  [실패] 192.168.0.99 - 1회 실패 (5회 초과 시 차단 시작)
  ...
  [시도 5회]
  [실패] 192.168.0.99 - 5회 연속 실패 → 1초 차단
  → 공격자가 1초 대기 후 재시도한다고 가정

  [시도 6회]
  [실패] 192.168.0.99 - 6회 연속 실패 → 2초 차단
  ...
  [시도 10회]
  [실패] 192.168.0.99 - 10회 연속 실패 → 32초 차단

=======================================================
IP별 최종 실패 기록
=======================================================
  192.168.0.1  | 누적 실패: 0회
  192.168.0.99 | 누적 실패: 10회
```

## 파일 구성

```
.
├── login_protector.py   # 메인: bcrypt 검증 + 지수 백오프 차단 알고리즘
└── .venv/               # 가상환경 (bcrypt)
```
